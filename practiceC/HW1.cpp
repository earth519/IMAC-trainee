#include <stdio.h>

main() {
  int number,x;
    printf("Please type in the number: ");
    scanf("%d",&number);
    for (int x = 0; x < number; x++) {
        /* code */
          printf("*");
        }
}
